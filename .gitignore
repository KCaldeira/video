# Ignore everything by default
*

# But allow these file types
!*.py
!*.txt
!*.md
!*.json
!*.xsd

# Allow .gitignore itself
!.gitignore

# Allow directories (so we can navigate into them)
!*/

# But ignore all files in directories (except the allowed extensions)
**/*
!**/
!**/*.py
!**/*.txt
!**/*.md
!**/*.json
!**/*.xsd

# Explicitly ignore virtual environments
.venv/
venv/
env/
ENV/

# Ignore all data directories
data/input/
data/output/
